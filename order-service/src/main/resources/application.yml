server:
  port: 8082

spring:
  application:
    name: order-service

discovery:
  server-url: http://localhost:8080
  service-name: order-service
  service-port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health